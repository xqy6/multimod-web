services:
  app:
    build: .
    ports:
      - "4000:4000"
    environment:
      PORT: 4000
      DATA_DIR: ./data
      ADMIN_USERNAME: admin
      ADMIN_PASSWORD: change-me
    volumes:
      - app-data:/app/server/data
    restart: unless-stopped

volumes:
  app-data:
